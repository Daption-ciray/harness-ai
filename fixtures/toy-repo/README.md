# toy-repo

A deliberately small repository used as a harness-ai integration fixture.
Tests copy it into a temp directory and `git init` there, so no nested git
repository ever lives inside harness-ai itself.
